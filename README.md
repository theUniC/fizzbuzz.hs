# FizzBuzz kata written in Haskell

This is a possible implementation of the FizzBuzz kata written in Haskell.

## How to run it

```bash
$ cabal sandbox init
$ cabal install --enable-tests
$ cabal test
``` 
